<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item active">Report Found Item</li>
                </ol>
            </nav>
            
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-hand-holding-heart me-2"></i> Report Found Item
                    </h4>
                </div>
                <div class="card-body p-4">
                    <p class="text-muted mb-4">
                        Thank you for helping! Fill out the form below to report a found item. 
                        Your report will be reviewed by an admin before being published.
                    </p>
                    
                    <form action="/report/found" method="POST" enctype="multipart/form-data">
                        <!-- Item Details -->
                        <h5 class="mb-3 border-bottom pb-2">
                            <i class="fas fa-box me-2"></i> Item Details
                        </h5>
                        
                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label">Item Name <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="itemName" required 
                                       placeholder="e.g., Blue Backpack, iPhone 13, etc." maxlength="100">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Category <span class="text-danger">*</span></label>
                                <select class="form-select" name="category" required>
                                    <option value="">Select Category</option>
                                    <% categories.forEach(function(cat) { %>
                                        <option value="<%= cat._id %>"><%= cat.name %></option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="col-12">
                                <label class="form-label">Description <span class="text-danger">*</span></label>
                                <textarea class="form-control" name="description" rows="4" required 
                                          placeholder="Describe the item in detail (color, brand, size, distinguishing features, etc.)"
                                          maxlength="1000"></textarea>
                                <small class="text-muted">Max 1000 characters</small>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Location Found <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="location" required
                                       placeholder="e.g., Library, Cafeteria, Building A" maxlength="200">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Date Found <span class="text-danger">*</span></label>
                                <input type="date" class="form-control" name="dateLostFound" required 
                                       max="<%= new Date().toISOString().split('T')[0] %>">
                            </div>
                            <div class="col-12">
                                <label class="form-label">Item Image (Optional)</label>
                                <input type="file" class="form-control" name="image" accept="image/*">
                                <small class="text-muted">Accepted formats: JPG, PNG, GIF, WebP. Max size: 5MB</small>
                            </div>
                        </div>
                        
                        <!-- Contact Information -->
                        <h5 class="mb-3 border-bottom pb-2">
                            <i class="fas fa-user me-2"></i> Contact Information
                        </h5>
                        
                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label">Your Name <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="reporterName" required 
                                       value="<%= typeof user !== 'undefined' && user ? user.username : '' %>"
                                       placeholder="Enter your full name" maxlength="100">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Your Email <span class="text-danger">*</span></label>
                                <input type="email" class="form-control" name="reporterEmail" required 
                                       value="<%= typeof user !== 'undefined' && user ? user.email : '' %>"
                                       placeholder="Enter your email address">
                            </div>
                            <div class="col-12">
                                <label class="form-label">Additional Contact Info <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="contactInfo" required 
                                       placeholder="Phone number or alternative contact method" maxlength="200">
                            </div>
                        </div>
                        
                        <!-- Terms -->
                        <div class="form-check mb-4">
                            <input class="form-check-input" type="checkbox" id="terms" required>
                            <label class="form-check-label" for="terms">
                                I confirm that the information provided is accurate and I understand that my report 
                                will be reviewed before publishing.
                            </label>
                        </div>
                        
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="fas fa-paper-plane me-2"></i> Submit Report
                            </button>
                            <a href="/" class="btn btn-outline-secondary btn-lg">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
