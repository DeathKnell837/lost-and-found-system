<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        @page {
            size: A4;
            margin: 0;
        }
        
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: white;
            color: #333;
        }
        
        .poster {
            width: 210mm;
            min-height: 297mm;
            margin: 0 auto;
            padding: 20mm;
            background: white;
        }
        
        .header {
            text-align: center;
            padding-bottom: 15mm;
            border-bottom: 4px solid #e74c3c;
            margin-bottom: 10mm;
        }
        
        .type-badge {
            display: inline-block;
            padding: 8px 25px;
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 3px;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        
        .type-lost {
            background: #e74c3c;
            color: white;
        }
        
        .type-found {
            background: #27ae60;
            color: white;
        }
        
        .title {
            font-size: 36px;
            font-weight: bold;
            color: #2c3e50;
            margin: 15px 0;
        }
        
        .subtitle {
            font-size: 18px;
            color: #7f8c8d;
        }
        
        .main-content {
            display: flex;
            gap: 20mm;
            margin-bottom: 15mm;
        }
        
        .image-section {
            flex: 1;
        }
        
        .item-image {
            width: 100%;
            max-height: 120mm;
            object-fit: cover;
            border-radius: 10px;
            border: 3px solid #ecf0f1;
        }
        
        .no-image {
            width: 100%;
            height: 120mm;
            background: #ecf0f1;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: #bdc3c7;
        }
        
        .details-section {
            flex: 1;
        }
        
        .detail-item {
            margin-bottom: 12px;
            padding: 10px;
            background: #f8f9fa;
            border-radius: 8px;
        }
        
        .detail-label {
            font-size: 12px;
            text-transform: uppercase;
            color: #95a5a6;
            font-weight: bold;
            margin-bottom: 3px;
        }
        
        .detail-value {
            font-size: 18px;
            color: #2c3e50;
        }
        
        .description-section {
            padding: 15px;
            background: #f8f9fa;
            border-radius: 10px;
            margin-bottom: 15mm;
        }
        
        .description-section h3 {
            font-size: 14px;
            text-transform: uppercase;
            color: #95a5a6;
            margin-bottom: 8px;
        }
        
        .description-section p {
            font-size: 16px;
            line-height: 1.6;
        }
        
        .qr-section {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 15px;
            background: #2c3e50;
            color: white;
            border-radius: 10px;
            margin-bottom: 10mm;
        }
        
        .qr-text {
            flex: 1;
        }
        
        .qr-text h3 {
            font-size: 20px;
            margin-bottom: 5px;
        }
        
        .qr-text p {
            font-size: 14px;
            color: #bdc3c7;
            word-break: break-all;
        }
        
        .qr-code {
            background: white;
            padding: 10px;
            border-radius: 8px;
        }
        
        .qr-code img {
            display: block;
        }
        
        .footer {
            text-align: center;
            padding-top: 10mm;
            border-top: 2px solid #ecf0f1;
        }
        
        .footer h2 {
            color: #6c5ce7;
            font-size: 24px;
            margin-bottom: 5px;
        }
        
        .footer p {
            color: #7f8c8d;
            font-size: 14px;
        }
        
        .print-btn {
            position: fixed;
            bottom: 20px;
            right: 20px;
            padding: 15px 30px;
            font-size: 18px;
            background: #6c5ce7;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(108, 92, 231, 0.3);
        }
        
        .print-btn:hover {
            background: #5b4cdb;
        }
        
        @media print {
            .print-btn { display: none; }
            body { background: white; }
        }
    </style>
</head>
<body>
    <div class="poster">
        <header class="header">
            <span class="type-badge type-<%= item.type %>">
                <%= item.type === 'lost' ? '🔍 LOST ITEM' : '✅ FOUND ITEM' %>
            </span>
            <h1 class="title"><%= item.title %></h1>
            <p class="subtitle">
                <% if (item.type === 'lost') { %>
                    Please help find this item!
                <% } else { %>
                    Is this yours? Claim it now!
                <% } %>
            </p>
        </header>

        <div class="main-content">
            <div class="image-section">
                <% if (item.image) { %>
                    <img src="<%= item.image %>" alt="<%= item.title %>" class="item-image">
                <% } else { %>
                    <div class="no-image">No Image Available</div>
                <% } %>
            </div>

            <div class="details-section">
                <div class="detail-item">
                    <div class="detail-label">📁 Category</div>
                    <div class="detail-value"><%= item.category ? item.category.name : 'General' %></div>
                </div>
                
                <div class="detail-item">
                    <div class="detail-label">📍 Location</div>
                    <div class="detail-value"><%= item.location %></div>
                </div>
                
                <div class="detail-item">
                    <div class="detail-label">📅 Date</div>
                    <div class="detail-value">
                        <%= new Date(item.dateFound || item.dateLost || item.createdAt).toLocaleDateString('en-US', {
                            weekday: 'long',
                            year: 'numeric',
                            month: 'long',
                            day: 'numeric'
                        }) %>
                    </div>
                </div>
                
                <% if (item.color) { %>
                    <div class="detail-item">
                        <div class="detail-label">🎨 Color</div>
                        <div class="detail-value"><%= item.color %></div>
                    </div>
                <% } %>
                
                <% if (item.brand) { %>
                    <div class="detail-item">
                        <div class="detail-label">🏷️ Brand</div>
                        <div class="detail-value"><%= item.brand %></div>
                    </div>
                <% } %>
            </div>
        </div>

        <% if (item.description) { %>
            <div class="description-section">
                <h3>📝 Description</h3>
                <p><%= item.description %></p>
            </div>
        <% } %>

        <div class="qr-section">
            <div class="qr-text">
                <h3>Scan QR Code for Details</h3>
                <p><%= itemUrl %></p>
            </div>
            <div class="qr-code">
                <img src="<%= qrCode %>" alt="QR Code" width="150" height="150">
            </div>
        </div>

        <footer class="footer">
            <h2>🔎 Campus Lost & Found</h2>
            <p>Helping reunite people with their belongings</p>
        </footer>
    </div>

    <button class="print-btn" onclick="window.print()">
        🖨️ Print Poster
    </button>
</body>
</html>
