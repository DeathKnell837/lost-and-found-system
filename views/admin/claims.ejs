<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <style>
        .claim-row { transition: all 0.2s; cursor: pointer; }
        .claim-row:hover { background: #f8f9fa; }
        .status-badge { padding: 0.3rem 0.7rem; border-radius: 15px; font-size: 0.75rem; }
        .priority-high { border-left: 4px solid #e74c3c; }
        .priority-normal { border-left: 4px solid #f39c12; }
        .priority-low { border-left: 4px solid #95a5a6; }
    </style>
</head>
<body>
    <div class="d-flex">
        <%- include('../partials/admin-sidebar') %>

        <main class="flex-grow-1 p-4">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h4 class="mb-1"><i class="fas fa-hand-holding me-2"></i>Claim Requests</h4>
                    <p class="text-muted mb-0">Review and manage item claim requests</p>
                </div>
            </div>

            <!-- Stats Cards -->
            <div class="row g-3 mb-4">
                <div class="col-md-2">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body text-center">
                            <h3 class="mb-0"><%= counts.all %></h3>
                            <small class="text-muted">Total</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card border-0 shadow-sm border-warning">
                        <div class="card-body text-center">
                            <h3 class="mb-0 text-warning"><%= counts.pending %></h3>
                            <small class="text-muted">Pending</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card border-0 shadow-sm border-info">
                        <div class="card-body text-center">
                            <h3 class="mb-0 text-info"><%= counts.under_review %></h3>
                            <small class="text-muted">Reviewing</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card border-0 shadow-sm border-success">
                        <div class="card-body text-center">
                            <h3 class="mb-0 text-success"><%= counts.approved %></h3>
                            <small class="text-muted">Approved</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card border-0 shadow-sm border-danger">
                        <div class="card-body text-center">
                            <h3 class="mb-0 text-danger"><%= counts.rejected %></h3>
                            <small class="text-muted">Rejected</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Filters -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-body">
                    <form class="row g-3 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label">Status</label>
                            <select name="status" class="form-select" onchange="this.form.submit()">
                                <option value="all" <%= currentStatus === 'all' ? 'selected' : '' %>>All Claims</option>
                                <option value="pending" <%= currentStatus === 'pending' ? 'selected' : '' %>>Pending</option>
                                <option value="under_review" <%= currentStatus === 'under_review' ? 'selected' : '' %>>Under Review</option>
                                <option value="approved" <%= currentStatus === 'approved' ? 'selected' : '' %>>Approved</option>
                                <option value="rejected" <%= currentStatus === 'rejected' ? 'selected' : '' %>>Rejected</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Sort By</label>
                            <select name="sort" class="form-select" onchange="this.form.submit()">
                                <option value="newest" <%= currentSort === 'newest' ? 'selected' : '' %>>Newest First</option>
                                <option value="oldest" <%= currentSort === 'oldest' ? 'selected' : '' %>>Oldest First</option>
                                <option value="priority" <%= currentSort === 'priority' ? 'selected' : '' %>>Priority</option>
                            </select>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Claims Table -->
            <div class="card border-0 shadow-sm">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="bg-light">
                            <tr>
                                <th>Item</th>
                                <th>Claimant</th>
                                <th>Status</th>
                                <th>Submitted</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (claims.length === 0) { %>
                                <tr>
                                    <td colspan="5" class="text-center py-5 text-muted">
                                        <i class="fas fa-inbox fa-3x mb-3 d-block"></i>
                                        No claims found
                                    </td>
                                </tr>
                            <% } else { %>
                                <% claims.forEach(claim => { %>
                                    <tr class="claim-row priority-<%= claim.priority %>">
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <% if (claim.item && claim.item.image) { %>
                                                    <img src="<%= claim.item.image %>" width="45" height="45" class="rounded me-2" style="object-fit: cover;">
                                                <% } else { %>
                                                    <div class="bg-light rounded me-2 d-flex align-items-center justify-content-center" style="width: 45px; height: 45px;">
                                                        <i class="fas fa-box text-muted"></i>
                                                    </div>
                                                <% } %>
                                                <div>
                                                    <div class="fw-medium"><%= claim.item ? claim.item.title : 'Deleted Item' %></div>
                                                    <small class="text-muted"><%= claim.item ? claim.item.location : 'N/A' %></small>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <div><%= claim.claimant.username %></div>
                                            <small class="text-muted"><%= claim.claimant.email %></small>
                                        </td>
                                        <td>
                                            <% if (claim.status === 'pending') { %>
                                                <span class="status-badge bg-warning text-dark">Pending</span>
                                            <% } else if (claim.status === 'under_review') { %>
                                                <span class="status-badge bg-info text-white">Reviewing</span>
                                            <% } else if (claim.status === 'approved') { %>
                                                <span class="status-badge bg-success">Approved</span>
                                            <% } else if (claim.status === 'rejected') { %>
                                                <span class="status-badge bg-danger">Rejected</span>
                                            <% } else { %>
                                                <span class="status-badge bg-secondary">Withdrawn</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <div><%= new Date(claim.createdAt).toLocaleDateString() %></div>
                                            <small class="text-muted"><%= new Date(claim.createdAt).toLocaleTimeString() %></small>
                                        </td>
                                        <td>
                                            <a href="/admin/claims/<%= claim._id %>" class="btn btn-sm btn-primary">
                                                <i class="fas fa-eye me-1"></i>Review
                                            </a>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
